# Raw OurAirports export - 12MB, not needed at runtime (airports.js is already built
# from it and committed). Re-download from https://ourairports.com/data/ locally if needed.
airports.csv

__pycache__/

# local dev-server config for browser testing, not part of the deployed site
.claude/
