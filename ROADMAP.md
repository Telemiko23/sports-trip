# Roadmap - מתכנן טיולי כדורגל

מסמך מעקב אחר התוכנית של הפרויקט: מה הוגדר בהתחלה, מה כבר נבנה, ומה מתוכנן קדימה.
מעודכן ידנית בכל פעם שמוסיפים משהו משמעותי לתוכנית.

## המטרה המקורית

כלי אישי לתכנון טיולי כדורגל באירופה:
- `build_fixtures.py` מושך משחקים עתידיים מ-API-Football (תוכנית Pro) עבור 7 מדינות
  (אנגליה, ספרד, גרמניה, צרפת, הולנד, איטליה, פולין) ו-23 ליגות/גביעים, מאתר מיקום לכל
  עיר דרך OpenStreetMap Nominatim, וכותב הכול ל-`fixtures.js`.
- `index.html` הוא אתר עברי (RTL) שקורא את `fixtures.js` ומאפשר לסנן משחקים לפי עיר
  בסיס ורדיוס, תאריכים, ימים בשבוע ותחרויות; לבנות מהם "טיול" עם חישוב מרחקים/זמנים בין
  משחקים; ולפתוח קישורי חיפוש לטיסות ולינה.

## בוצע

**סבב 1:**
- תרגום מלא לעברית: תחרויות, ערים, שמות קבוצות - מילון ידני (`he_names.py`) + תעתוק
  פונטי אוטומטי כגיבוי לכל שם עתידי שלא במילון. מחובר גם ל-`build_fixtures.py` (כל
  הרצה עתידית) וגם רץ כ-`backfill_hebrew.py` חד-פעמי על נתונים קיימים בלי לפגוע במכסת ה-API.
- תיקון באג כיוון טקסט (RTL/LTR) שגרם להיפוך חזותי של "בית – חוץ" כששם קבוצה מתחיל
  באות לועזית.
- הסרת "הסתר משחקים בלי שעה מאושרת" (אין בנתונים אות אמין להבדיל בין שעה מאושרת לברירת מחדל).
- הסרת "מקסימום ק"מ בין משחקים" והמספר בק"מ בין משחקים בטיול (המרחק הכולל וכל מרחק ביניים) -
  נשארה רק אינדיקציית "אותו יום / למחרת / X ימים אחר כך" ואזהרת חפיפה בשעות.
- השלמה אוטומטית לעיר בסיס (מהנתונים עצמם) ולשדה "טיסה מ-" (רשימת ערים).
- תמיכה בלוגואים של קבוצות (השדה כבר קיים ב-API-Football).
- ריענון עיצוב: כרטיסים עם צל, טיפוגרפיה וצבעים מחודדים יותר, מצב בהיר/כהה.

**סבב 2:**
- תיקון באג נוסף בכיוון טקסט: כש-`<bdi>` מזהה כיוון אוטומטית, שם קבוצה שמתחיל באות
  לועזית (כמו "AFC") הפך את כל השורה (כולל מיקום הלוגואים) ל-LTR במקום RTL - גם כשרוב
  הטקסט עברי. התיקון: `dir="rtl"` מפורש בכל מקום, לא הסתמכות על ניחוש.
- ריווח בין לוגו לשם קבוצה.
- הוספת שם אצטדיון לכל משחק (בתוצאות ובטיול) - השדה כבר נאסף מה-API, רק לא הוצג.
- מילון עברית: תוקנו שגיאות תעתיק (Leyton Orient, West Brom, Wimbledon, Waltham Abbey
  ועוד), נוספו ~60 קבוצות ו-~30 ערים שהתגלו בהרצה חדשה (סבבי מוקדמות של גביע ה-FA).
  התהליך שכתב את `he_names.py` נבדק כעת אוטומטית מול `fixtures.js` בכל עדכון, כדי
  שלא יישארו פערים בשקט.
- תיקיית `logos/` מקומית: `download_logos.py` מוריד כל לוגו פעם אחת ומחליף את ה-URL
  המרוחק בנתיב מקומי - רץ אוטומטית בסוף כל הרצה של `build_fixtures.py`, וגם ניתן
  להרצה עצמאית.
- רשימת "טיסה מ-" הוחלפה לרשימת שדות תעופה אמיתיים מתוך מאגר OurAirports (`airports.csv`
  שסופק, מעובד ע"י `build_airports.py` ל-`airports.js`) - מסונן לשדות עם טיסות סדירות
  ומסחריות בלבד (`scheduled_service=yes`, יש קוד IATA, וסוג large/medium/small) - כ-4,000
  שדות תעופה ברחבי העולם, בדיוק כמו באתרי טיסות.
- טיסות multi-city: כשעיר המשחק הראשון בטיול שונה מעיר המשחק האחרון, נפתחים שני קישורי
  טיסה חד-כיוונית (הלוך + חזור) במקום טיסת הלוך-חזור רגילה לאותה עיר.

**סבב 3:**
- באג נוסף באותה משפחה של כיוון טקסט: כשלוגו הקבוצה יושב בתוך אותו `<bdi>` יחד עם השם,
  והשם מתחיל באות לועזית (כמו "AFC"), הלוגו "קפץ" לצד ההפוך. נבדק ואומת ב-pixel position
  אמיתי (לא רק חזותית) שהתיקון `dir="rtl"` מפורש פותר את זה סופית.
- מילון עברית: כ-30 קבוצות/ערים בספרד והולנד תוקנו/הושלמו לפי רשימה מפורטת (שם קבוצה,
  עיר בסיס, שם אצטדיון, לוגו) - כולל ערים חדשות שהצריכו גיאוקוד (Ourense, Soria, Alcázar
  de San Juan, Tavernes de la Valldigna, San Pedro del Pinatar, Talayuela, Ribadesella,
  Groesbeek). נוסף מנגנון `overrides.py` (עיר/אצטדיון/לוגו לפי שם קבוצה) שמחליף או משלים
  את מה שה-API מחזיר - נפרד מ-`he_names.py` שהוא תרגום בלבד.
- זוהו והוחלפו 5 לוגואים גנריים ("OFFICIAL LOGO SOON" / "image not available") בלוגואים
  אמיתיים שסופקו ידנית, בעזרת סריקת perceptual-hash אוטומטית על כל 468 הלוגואים (רק 5
  התאימו לתבנית placeholder - כך שזו לא בעיה שיטתית, רק במקרים שסומנו).
- קובצי הלוגו בתיקיית `logos/` הוסבו משם מספרי (למשל `1333.png`) לשם הקבוצה באנגלית
  (`AFC_Wimbledon.png`) כדי שהתיקייה תהיה קריאה.
- לרשימת "טיסה מ-" נוספה עמודת `municipality_he` ל-`airports.csv` (כ-86,000 שורות,
  `add_hebrew_city_column.py`) כך שאפשר לחפש שדה תעופה גם לפי שם העיר בעברית בלי להחליף
  שפה - המילון של הפרויקט עצמו (`he_names.py`) משמש בסיס, ותעתוק פונטי כגיבוי לכל עיר
  בעולם שלא בו.
- הדרופדאונים של "עיר בסיס" ו"טיסה מ-" הוחלפו מ-`<datalist>` הדפדפן (שלא ניתן לעצב) לרכיב
  autocomplete עצמאי (`combobox()` ב-JS) עם עיצוב תואם לאתר, הדגשת ההתאמה בטקסט, וניווט
  במקלדת.
- ריענון עיצוב שני: כותרת "hero" ירוקה עם דוגמת קווי מגרש, מבטא זהב לכפתורים ולנקודות
  מרכזיות, צבע מדינה (פס + תג) לכל משחק ברשימה לסריקה מהירה יותר.
- **בדקתי בפועל מול Google Flights: אין דרך אמינה לפתוח ישירות במצב Multi-city עם ערים
  ממולאות מראש דרך URL ציבורי/מתועד.** בדיקה חיה הראתה שה-query הטבעי (`?q=...`) שעובד
  מצוין ל-One-way/Round-trip פשוט נכשל לגמרי כשניסיתי ניסוח multi-city - נשאר עם דף
  הבית הריק. לכן נשאר המצב הנוכחי (שני קישורי הלוך/חזור נפרדים) כפתרון הכי טוב שאומת
  בפועל, למרות שהמשתמש ביקש כפתור אחד - מתועד כאן כדי לא לנסות שוב את אותו דבר בעתיד
  בלי סיבה חדשה לחשוב שזהישתנה (אולי דרך API בתשלום של Google, לא נבדק).

**סבב 4:**
- `overrides.py` הורחב: `TEAM_CITY_OVERRIDE` הפך ל"תמיד מנצח" (לא רק כשחסרה עיר) כדי לתקן
  גם עיר שגויה שה-API מחזיר (למשל Paris FC קיבל `venue.city="Isilemulino"` - לא מקום
  אמיתי - במקום פריז).
- נוסף `missing_locations_report.py`: מריצים אחרי כל `build_fixtures.py` כדי לראות אילו
  ערים יש להן שם אבל לא נ.צ. (ומופיעות בהודעת "משחקים בלי מיקום מזוהה") - ותיקון שתיים
  שכבר נמצאו (Crosby/Merseyside דרך `cities_manual.json`, Paris FC דרך override).
- לוגואים של תחרויות (ליגות/גביעים): נוסף `comp_logos/` + `download_comp_logos()`, רץ
  אוטומטית כמו לוגואי הקבוצות. מוצגים ליד תג התחרות בתוצאות ובתפריט הסינון.
- קיבוץ שדות תעופה לפי עיר: ל-77 ערים עם 2+ שדות תעופה (למשל לונדון) נוספה אופציית
  "כל שדות התעופה" אחת בראש התוצאות, כדי לא להצטרך לדעת איזה שדה ספציפי.
- תיקוני CSS: אייקון לוח השנה בשדות התאריך הובהר (במיוחד במצב כהה), ימי השבוע בסינון
  ותצוגת קבוצה+לוגו בטיול לא נשברים יותר לשורה נוספת (הרחבתי את פאנל "הטיול שלי" בשביל זה).

**סבב 5:**
- **תמיכה בליגת האלופות / ליגת האירופה / ליגת הקונפרנס** - זה בוצע, אבל **עדיין לא נבדק
  מול ה-API האמיתי** (אין לי מפתח) - צריך שתריץ `build_fixtures.py` ותבדוק את שלוש
  השורות המודפסות שמתחילות ב-`UEFA` כדי לוודא שזה עובד כמצופה:
  - במקום ID מנוחש, `resolve_uefa_id()` מוצא את ה-ID האמיתי בזמן ריצה דרך `leagues?search=`
    (אותו רעיון כמו שכבר עשינו ל-Puchar Polski) - כדי לא להסתמך על מספר לא מאומת.
  - משחק בתחרות אירופית נשמר רק אם אחת הקבוצות (בית או חוץ) כבר מופיעה באחת מ-23
    התחרויות המקומיות שלנו - כך ריאל מדריד-שחטאר יישמר (ריאל כבר אצלנו) גם אם שחטאר לא.
  - מיקום המשחק נגזר מגיאוקוד **בלי** הגבלת מדינה (`geocode_any()`, עם `addressdetails=1`
    מ-Nominatim) - כדי לתפוס נכון מקרה כמו שחטאר שמארח באצטדיון ניטרלי, לא במדינה שלו.
  - שעת המשחק מומרת לאזור הזמן האמיתי של המגרש (מיפוי קוד מדינה → אזור זמן) רק אחרי
    שידוע המיקום - אי אפשר לדעת מראש כמו בתחרויות המקומיות.
  - עלות: עד 6 בקשות API נוספות (2 ניסיונות עונה × 3 תחרויות) + גיאוקוד לערים חדשות.
  - **עודכן**: נבדק בפועל אחרי הרצה חיה - 208 משחקים רלוונטיים נמצאו (95 ליגת אלופות,
    77 אירופה, 36 קונפרנס), כולל שחטאר דונייצק כמו בדוגמה המקורית. גם נמצא ותוקן באג
    אמיתי: חיפוש גיאוקוד בלי הגבלת מדינה בכלל התבלבל בין ערים עם שם זהה במדינות שונות
    (Athens → תפס את ג'ורג'יה שבארה"ב במקום יוון) - הפתרון: קודם מגבילים לרשימת 55
    מדינות ה-UEFA, ורק אם זה לא מוצא כלום נופלים לחיפוש גלובלי לגמרי.
  - **תיקון נוסף**: סקוטלנד/ויילס/צפון אירלנד כולן מחזירות `country_code=gb` מ-Nominatim
    (אין קוד נפרד), כך שקבוצה סקוטית (למשל Celtic) הייתה מסווגת בטעות תחת "אנגליה" -
    עכשיו נבדק שדה `ISO3166-2-lvl4` בתשובה כדי להבדיל ביניהן.

**סבב 7:**
- **תצוגת מפה** - נוסף מצב תצוגה שני לתוצאות ("רשימה" / "מפה", כפתור מעבר מעל הרשימה),
  עם מפה אינטראקטיבית (Leaflet + OpenStreetMap tiles, ללא צורך במפתח API - בדיוק כמו
  שתועד כאן כמועמד טבעי). כל עיר עם משחקים בטווח הסינון הנוכחי (תאריכים/ימים/תחרויות)
  מסומנת בפין עם מספר המשחקים בה; לחיצה על פין פותחת פופאפ עם רשימת המשחקים באותה עיר
  (תאריך, שעה, קבוצות, תג תחרות, אצטדיון) וכפתור "הוסף לטיול" ישירות מהמפה - בלי לחזור
  לתצוגת הרשימה. כשיש עיר בסיס מוגדרת, מצויר מעגל הרדיוס סביבה והמפה ממורכזת אליו;
  אחרת המפה מתאימה את עצמה (fit bounds) לכל הערים המסוננות. נבדק בפועל בדסקטופ ובמובייל
  (375px) - עובד תקין, אין שגיאות קונסול.
- תיקון: Lillestrøm (הקבוצה הנורווגית) לא קיבלה מיקום מזוהה בכלל, למרות שהיה לה override
  מוגדר - כי ה-API מחזיר את שם הקבוצה כ-"Lillestrom" (ASCII, בלי ø) בעוד שה-override
  והמילון העברי היו ממופתחים עם "Lillestrøm" (עם ø) - אז ההתאמה מעולם לא הייתה מדויקת.
  תוקן בשלושת המקומות (he_names.py, TEAM_CITY_OVERRIDE, VENUE_OVERRIDE) לפי המחרוזת
  המדויקת שה-API מחזיר בפועל (אומת ישירות מול fixtures.js, לא ניחוש).
- נוסף "Slavia Praha" למילון העברית (סלביה פראג) - לא היה במילון בכלל קודם, ולכן נפל
  לתעתיק פונטי גולמי.

**סבב 6:**
- באג ארכיטקטוני ב-`backfill_hebrew.py`: כשמוסיפים override חדש (עיר/וודוא) לקבוצה
  שכבר הייתה לה נ.צ. (ולו שגוי) מהרצה קודמת, הסקריפט לא היה מרענן את הנ.צ./המדינה כי
  התנאי היה "רק אם עדיין אין נ.צ. בכלל". עכשיו override תמיד גורם לרענון מלא (נ.צ. +
  מדינה), ומשחק בליגה אירופית תמיד מוודא מחדש את המדינה שלו דרך אותו קאש - כך שתיקון
  כמו סקוטלנד/אנגליה חל גם על נתונים ישנים בלי להצטרך להריץ מול ה-API מהתחלה.
- `backfill_hebrew.py` מייבא עכשיו ישירות פונקציות/קבועים מ-`build_fixtures.py`
  (`geocode`, `geocode_any`, `CC_TO_OUR_COUNTRY` וכו') במקום לשכפל את הלוגיקה - כדי
  שתיקון עתידי ב-build_fixtures.py יחול אוטומטית גם כאן.
- תעתוק פונטי: נוספה תמיכה באותיות סקנדינביות ø/å/æ (לדוגמה "Bodø" הפך לג'יבריש לפני
  התיקון).
- תג תחרות (עם לוגו) נוסף גם לכרטיסי המשחק בתוך "הטיול שלי", לא רק ברשימת התוצאות.
- כותרת "הטיול שלי" שונתה מ"X משחקים · Y ימי משחק" ל"X אירועים ב-Z ימים", כש-Z = טווח
  הימים מהמשחק הראשון עד המשחק האחרון ועוד יום (הנחה שהטיסה חזרה תהיה למחרת המשחק
  האחרון) - כולל מקרה קצה של כמה משחקים באותו יום.
- כפתור "הכל / כלום" נוסף גם לכל התחרויות ביחד, לא רק פר-מדינה.
- כ-45 תיקוני שם/עיר/אצטדיון לקבוצות בליגות האירופיות, לפי רשימה מפורטת מהמשתמש -
  חלקן חשפו עוד תקלות דאטה אמיתיות (למשל Hapoel Beer Sheva קיבלה עיר "Bucharest" מה-API
  במקום באר שבע, Union St. Gilloise קיבלה את המגרש של קבוצה אחרת) שתוקנו באותו מהלך.

**סבב 8:**
- **נ.צ. (קואורדינטות) מדויק לכל אצטדיון** - הושלם. גיאוקוד נפרד לשם האצטדיון עצמו (לא רק
  לעיר) דרך Nominatim, עם קאש עצמאי (`venues_cache.json`, אותו רעיון כמו `cities_cache.json`)
  כדי שכל אצטדיון נבדק פעם אחת בלבד. כיסוי בהרצה הראשונה: 301 מתוך 346 אצטדיונים נמצאו
  (2,040 מתוך 2,415 משחקים) - השאר נופלים בחזרה לחיפוש טקסט רגיל בלי נ.צ. מדויק.
  - שם האצטדיון בכל מקום באתר (רשימה, "הטיול שלי", פופאפ במפה) הפך לקישור שפותח את
    Google Maps ממוקד בדיוק על האצטדיון - קישור פשוט, בלי מפתח API ובלי עלות.
  - קישורי חיפוש הלינה הקיימים שודרגו להשתמש ב-`latitude`/`longitude`/`distance=3000`
    (פרמטרים ציבוריים ומתועדים של booking.com) כדי לחפש לינה סביב האצטדיון עצמו במקום
    רק לפי שם העיר, כשהמיקום המדויק ידוע.
  - **נבדק אבל לא מומש**: המלצות למקומות לינה ספציפיים (שם/דירוג/מחיר) ישירות באתר -
    ה-API של Hotellook/Booking דורש חתימת MD5 עם טוקן, מה שמרמז על שימוש משרת ולא
    ישירות מדפדפן באתר סטטי; יש אפשרות בטוחה יותר (widget מובנה של Travelpayouts) שעדיין
    לא נבדקה, וזה בכל מקרה תלוי באישור ה-Booking affiliate שעדיין ממתין.
  - תוך כדי: התגלה ותוקן עוד באג דאטה אמיתי - Hapoel Beer Sheva קיבלה מה-API שם אצטדיון
    שגוי לגמרי ("Superbet Arena - Giulesti", אצטדיון ברומניה) - תוקן ל-Turner Stadium
    דרך `VENUE_OVERRIDE`.
- **Google Maps (במקום Leaflet+OSM)** - נבדק ונדחה: Google דורשים חשבון Google Cloud עם
  כרטיס אשראי מחובר כדי בכלל להנפיק מפתח API (10,000 טעינות מפה בחינם בחודש, אח"כ $7
  ל-1,000), בניגוד ל-Leaflet+OSM הנוכחי שחינמי וללא מפתח לגמרי - **הוחלט להישאר עם
  Leaflet+OSM**.
- **אבטחה**: עבר סבב בדיקה שיטתי (ראה `STATUS.md` → "אבטחה") - נוסף Content-Security-Policy
  ו-Subresource Integrity על טעינת Leaflet מ-CDN. זו כעת פרקטיקה קבועה - כל שינוי קוד
  עתידי עובר גם הוא בדיקת אבטחה, לא רק כשמתבקש.
- **Google Analytics 4** - בתהליך הטמעה (ממתין ל-Measurement ID מהמשתמש) - events מתוכננים:
  הוספה/הסרה מהטיול (עם תחרות/עיר/מדינה), מעבר רשימה/מפה, לחיצה על עיר במפה, קליק על
  קישורי טיסה/לינה, שינוי עיר בסיס/רדיוס, בחירת שדה תעופה.
- באג נוסף מאותה משפחה כמו Lillestrøm (סבב 6): "Fenerbahce" (ASCII) במילון מול
  "Fenerbahçe" (עם ç) בפועל מה-API - תוקן. גם "Aston Villa" - תוקן איות "ברמינגהאם".
- תיקוני UX במפה: הוספת משחק מפופאפ במפה כבר לא מאפסת זום/מרכז ולא סוגרת את הפופאפ
  (המצב שמשפיע על התצוגה עכשיו נבדק בנפרד ממצב הטיול) - ופופאפים ארוכים גוללים בפנים
  (`maxHeight`) במקום לגלוש מעבר לגבולות המפה.

**סבב 9:**
- **Google Analytics 4** הוטמע (Measurement ID `G-BKHDQGMC8T`) - CSP עודכן לאפשר
  googletagmanager/google-analytics. events מפורטים לניתוח עתידי: הוספה/הסרה מהטיול
  (עם תחרות/מדינה/עיר/ימים-עד-המשחק/מקור-הלחיצה: רשימה/מפה/פאנל-טיול), מעבר רשימה↔מפה,
  לחיצה על עיר/אצטדיון במפה, קליק על קישורי טיסה/לינה/Google Maps, שינוי עיר
  בסיס/רדיוס/תאריכים, סימון/ביטול תחרות (בודדת/כל-מדינה/הכל), בחירת שדה תעופה, העתקת/ניקוי
  טיול, פתיחת חלון המידע המשפטי.
- **מספרי גרסה**: נוסף קבוע `APP_VERSION` ב-`index.html`, מוצג קטן בתחתית העמוד. סכמה:
  semver לא-רשמי - עדכון patch לתיקוני באגים/דאטה בלבד, minor לתכונה חדשה, major לשינוי
  כיוון גדול (כמו הרחבת רב-ספורט העתידית). גרסה נוכחית: **1.1.0**.
- **מידע משפטי מוסתר**: כל הטקסט המשפטי (הבהרת לוגואים/סימנים מסחריים, הצהרת
  Google Analytics, הבהרת affiliate) עבר מהפוטר הגלוי לחלון קופץ (`<dialog>`) שנפתח
  מקישור "מידע משפטי ופרטיות" - נשארו גלויים בפוטר רק קרדיטים שחובה להציג לפי תנאי
  השימוש של המקורות (API-Football/OSM/OurAirports).
- **מפה: פין נפרד לכל אצטדיון** (לא רק לעיר) - כשיש נ.צ. מדויק לאצטדיון, כל אצטדיון
  מקבל פין נפרד במפה (לדוגמה מדריד: הברנבאו והמטרופוליטנו נפרדים, לא פין אחד משותף
  לעיר) - כותרת הפופאפ מציגה את שם האצטדיון עצמו כשידוע.
- אקסלט של 44 האצטדיונים שלא אותרו יוצא למשתמש למילוי ידני (שם מתוקן לחיפוש /
  קואורדינטות ידניות) - לייבוא בחזרה ל-`overrides.py`/`cities_manual.json`-style כשיחזור.
- **נבדק ולא מומש (שיקול דעת המשתמש)**: הצגת מרחק בק"מ/זיהוי משחקים "קרובים הליכתית"
  בין ערים שונות רשמית ב"הטיול שלי" - זה ידרוש להחזיר תצוגת מרחק שהוסרה בכוונה בסבב 1
  ("אין צורך בו והוא כרגע סתם מסבך"); לא הופעל מחדש בלי אישור מפורש, אבל עכשיו עם נ.צ.
  מדויק לאצטדיון זה יהיה מדויק משמעותית יותר אם ירצו בעתיד.
- רעיון "שדה התעופה הקרוב לאצטדיון" - נשקל ונדחה (לא מעניין את המשתמש).

**סבב 10 (2026-09-24, עבודת לילה - עדיין לא אומת מול הרצה חיה):**
- **תיעוד events**: נוסף `ANALYTICS.md` - כל event, הפרמטרים שלו, מה מטריגר אותו, ולמה
  הוא טוב. גם נוסף `console.debug('[analytics]', ...)` לכל event כדי שאפשר יהיה לראות
  אותם חיים ב-DevTools Console בלי לחכות לדשבורד של Google.
- **44 האצטדיונים הושלמו**: המשתמש מילא קואורדינטות ידניות לכל ה-44 שנותרו (0 פערים
  כרגע). תוך כדי נמצא ותוקן עוד באג דאטה: Raków Częstochowa קיבלה מה-API שני שמות
  אצטדיון שונים לאותו מגרש פיזי ("Miejski Stadion Pilkarski Rakow" מול "zondacrypto
  Arena" - שם ספונסר ישן, אומת בחיפוש שהעיר הסירה אותו באפריל 2026) - תוקן ב-`VENUE_OVERRIDE`.
- **פורטוגל (Primeira Liga + Taça de Portugal) וליגת האומות (UEFA Nations League)
  נוספו** - **טרם אומתו מול ה-API החי** (אין מפתח כאן). שתי הליגות הפורטוגזיות מזוהות
  לפי שם בזמן ריצה (לא ID קשיח, כי לפרימיירה ליגה יש היסטוריית שמות ספונסר: ליגה
  בוואין/נוס/בטקליק). ליגת האומות נוספה כקטגוריה חדשה ("תחרויות נבחרות") שמשתמשת באותה
  לוגיקת "מיקום לא ידוע עד גיאוקוד" כמו התחרויות היבשתיות, אבל בודקת רלוונטיות לפי שמות
  נבחרות (בהנחה לא-מאומתת ששם הנבחרת ב-API זהה לשם המדינה, למשל "England") ולא לפי שמות
  קבוצות מועדון. **לבדוק בהרצה הראשונה**: שהמספרים בשורות `Intl`/`Portugal` בפלט אינם
  0 (0 = סימן שההנחות לא נכונות וצריך לתקן).
- **`translation_coverage_report.py` חדש** - סורק את `fixtures.js` הקיים ומוצא כל שם
  קבוצה/עיר שנפל לתעתיק פונטי אוטומטי במקום תרגום ידני שנבדק (אותו רעיון כמו
  `missing_locations_report.py`, בשביל תרגום ולא מיקום). הרצה על הדאטה החי (עוד לפני
  שפורטוגל/ליגת האומות בכלל קיימות) מצאה **28 קבוצות + 16 ערים** שמעולם לא נבדקו ידנית
  - כולן תוקנו. תוך כדי נמצאו שני באגים אמיתיים:
  - עוד מקרה של דפוס ה-ASCII-מול-דיאקריטיקה (כמו Lillestrøm/Fenerbahçe בסבבים קודמים):
    "Besiktas" (ASCII) במילון מול "Beşiktaş" (המחרוזת האמיתית מה-API) - תוקן.
  - **Heart of Midlothian (הארטס, קבוצה סקוטית) הוצגה תחת "אנגליה"** - כי ערך ישן במטמון
    (`"Edinburgh|gb"`, מלפני שנוסף הבידול סקוטלנד/אנגליה) מנע רענון. הוסר, ואדינבורו
    עכשיו מזוהה נכון כ"sct" ומוצג תחת סקוטלנד.
  - **נשאר לא פתור, דורש קלט מהמשתמש**: "Aarhus" (ליגת הקונפרנס) מוצגת עם עיר="Randers"
    ואצטדיון="Cepheus Park" - אומת בחיפוש ש-Cepheus Park הוא בפועל האצטדיון הביתי של
    Randers FC, לא של אוארהוס - כלומר יש כאן בלבול אמיתי בדאטה של ה-API, אבל לא ברור אם
    שם הקבוצה שגוי (וזו בעצם Randers FC) או שהעיר/אצטדיון שגויים (ואוארהוס שיחקה שם
    זמנית) - לא תוקן בניחוש.
- 0 פערי תרגום ו-0 מיקומים חסרים נכון לעכשיו (על הדאטה הקיים - פורטוגל/ליגת האומות
  יצריכו בדיקה משלהם ברגע שיהיה דאטה אמיתי).

**סבב 11 (2026-09-24, בוקר):**
- **מיתוג במקום אחד**: `brand.js` (שם המוצר + גרסה) - כותרת הדף, הכותרת בראש האתר והגרסה
  בפוטר נקראים משם. נבדק בפועל בדפדפן עם שם בדיקה (ואז הוחזר). מדריך לשינוי שם גם ב-GitHub
  / GA4 / Travelpayouts (כולל ההשלכות על כתובת האתר) ב-[BRANDING.md](BRANDING.md).
- **Booking.com נדחה** ב-Travelpayouts - מחקר על הסיבות האפשריות (קהל מתחת ל-1,000
  משתמשים פעילים בחודש, מעט תוכן מקורי, אי אפשר לאמת בעלות על תת-דומיין של github.io) ותוכנית
  הגשה חוזרת מפורטת ב-STATUS.md. לא נקבע מה הסיבה בפועל - היא כתובה ב-My Programs → Others.
- **AllSportDB** נסקר (ספק אירועי ספורט רב-ענפי, תוכנית חינמית): מתאים לתכנון טיולים
  (רמת אירוע/סופ"ש, לא משחק בודד), אבל מפתח הגישה החינמי פג כל 30 יום וידני, 10 אירועים
  לקריאה, בלי lat/lng ובלי קישורי כרטיסים - פירוט ב-MULTISPORT.md. נוסף
  `allsportdb_probe.py` (קריאה בלבד) לבדיקת כיסוי אמיתי לפני שבונים משהו.
- **תיקון להמלצה מאתמול**: TheSportsDB החינמי **לא** מתאים לדארטס - נבדק בפועל, יש רק 15
  אירועי 2026 האחרון מפברואר, ואפס אירועים עתידיים.
- **גרסה 1.2.0 - ענפי ספורט נוספים: פורמולה 1 וטניס** (לפי בחירת המשתמש, כולל אירועים
  מחוץ לאירופה). מקור: AllSportDB. סקריפט חדש `build_events.py` (לא בתוך ה-Action היומי - המפתח
  החינמי פג כל 30 יום; רץ ידנית, ו-`build_fixtures.py` נושא הלאה את האירועים העתידיים).
  27 אירועים נכון להיום (10 מרוצי F1 + 17 אירועי טניס), כולן עם תרגום עברי, נ.צ. (גאוקוד
  דרך Nominatim) ו-0 פערי תרגום. באתר: אירוע רב-יומי מוצג עם טווח תאריכים (`21/9–27/9`)
  ואייקון ענף במקום קבוצות/שעה; סינון תאריכים/ימים בשבוע חל אם *אחד* מימי האירוע
  מתאים; "הטיול שלי" מחשב לינה/טיסות לפי תחילת וסיום האירוע. הענפים מופיעים כקבוצות
  משלהם בתפריט התחרויות. שים לב: "גרנד פרי בחריין" נערך במלזיה (Sepang) בגלל מלחמת
  איראן - אומת, לא שגיאה. אירוע רב-עירוני (למשל United Cup) ממוקם בעיר הראשונה שלו.
- **עדיין ממתין**: אימות פורטוגל/ליגת האומות מול ה-API החי - ההרצה המתוזמנת לא רצה עדיין
  (יש רק שתי הרצות ידניות ברשימה); צריך להריץ ידנית מטאב Actions.

### מנוע המלצות טיולים
המלצה אוטומטית על טיולים על סמך הדאטה הקיים: רצף טוב של כמה משחקים פרוסים על ימים
רצופים באותו אזור גאוגרפי, עם חיפוש טיסות ולינה מוצע מראש. ידרוש לוגיקת "ניקוד" למסלולים
אפשריים (למשל: מספר משחקים / משך הטיול / מרחק כולל בין משחקים).

### חיפוש חכם בשפה חופשית (MCP)
רעיון מ-2026-09-24: לחשוף את נתוני האתר (משחקים/תחרויות/ערים/תאריכים) כשרת MCP, כדי
שאפשר יהיה לבקש טיול בשפה טבעית ("תכנן לי שבוע בספרד עם כמה שיותר משחקי לה ליגה") ולקבל
המלצה בפועל - במקום למלא טפסי סינון ידנית. זה בעצם ממשק/כניסה חלופית לאותה יכולת של
"מנוע המלצות הטיולים" שכבר ברשימה למעלה (לא תחליף לה - שכבה נוספת מעליה): מנוע ההמלצות
הוא הלוגיקה שבוחרת/מנקדת מסלולים, ו-MCP הוא הדרך "לדבר" איתה בשפה חופשית במקום דרך
תפריט הסינון. ידרוש מחשבה על: אילו tools לחשוף (חיפוש משחקים לפי קריטריונים, בניית טיול,
שליפת פרטי טיול קיים), האם זה משרת מקומי/דפדפני או שרת מרוחק שרץ לצד GitHub Pages
(GitHub Pages הוא static hosting בלבד - שרת MCP אמיתי ידרוש איפשהו אחר לרוץ, כנראה
serverless חינמי כמו Cloudflare Workers), ואבטחה (MCP server חשוף לציבור = משטח תקיפה
חדש, ידרוש אותה גישה שיטתית שכבר קיימת בפרויקט הזה - CSP, rate limiting וכו').

### קישורים לרכישת כרטיסים
הפניה למקום רלוונטי לרכישת כרטיסים לכל משחק (אתר המועדון / פלטפורמת כרטיסים לפי המדינה
והתחרות). ידרוש לוגיקה - אין API אחיד לכרטיסים כמו שיש לטיסות/מלונות, כך שכנראה יהיה
צריך מיפוי ידני לפי תחרות/מדינה (או קישור חיפוש כללי כמו שנעשה לטיסות ולינה) - וגם מחשבה
על עיצוב שלא יעמיס על כרטיס המשחק הקיים.

### לא רק כדורגל - תמיכה בענפי ספורט נוספים
**שינוי כיוון מהותי**: האתר לא יישאר ייעודי לכדורגל בלבד - המטרה היא לאפשר כל אירוע
ספורט שהיוזר ירצה להוסיף. משמעויות ארכיטקטוניות:
- **סינון נוסף לפי ענף ספורט** בתפריט הסינון, בנפרד מסינון לפי תחרות (מישהו שרוצה רק
  כדורגל, רק כדורסל, או שילוב).
- **לוגואים/תגיות ענף** לצד תגית התחרות הקיימת בכל משחק (איקונה של הענף, לא רק שם
  התחרות) - כנראה סט אייקונים קטן (⚽🏀🎾 וכו') במקום להסתמך על API-Football (שמכסה
  כדורגל בלבד).
- **מקור נתונים**: API-Football משרת כדורגל בלבד - ענף ספורט נוסף (כדורסל, טניס וכו')
  ידרוש API נפרד לגמרי (למשל API-Basketball מאותה משפחת API-Sports, אם רוצים להישאר
  עם ספק דומה) - כלומר `build_fixtures.py` יצטרך הרחבה לתמוך במספר מקורות, לא רק עוד
  תחרויות מאותו מקור כמו שעשינו לליגת האלופות.
- **מבנה הדאטה**: ככל הנראה יידרש שדה `sport` על כל fixture (כרגע מרומז - הכול כדורגל),
  וכנראה גם שינוי בשם/מבנה `fixtures.js` עצמו אם זה יגדל להיות "אירועים" כלליים ולא
  רק "משחקי כדורגל".

## איך לעדכן את המסמך הזה

כל פעם שמתחילים פיצ'ר גדול חדש או שסבב עבודה מסתיים - מוסיפים שורה תחת "בוצע" או
"בתכנון" בהתאם. אין צורך לתעד תיקוני באג קטנים או שינויי טקסט בודדים.
