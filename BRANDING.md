# מיתוג המוצר - ToSport

## מערכת העיצוב (v1.6.0, 2026-09-25) - ערכת מיתוג ToSport, ערכת נושא כהה

מקור: קובץ ההוראות `ToSport-Claude-Code-Branding-Prompt.md` + לוח הווריאציות של הלוגו. הערכים
הכתובים שם הם הסמכותיים (לא צבעים שנדגמו מתמונות). **האתר כהה בלבד** (בחירת המשתמש - הרקע הבהיר
מסנוור); מערכת הצבעים הבהירה מהלוח (paper #F8FAFC / surface #FFFFFF / muted #475569 / border
#E2E8F0) נשמרת כאן לעתיד, למקרה שיתווסף מתג ערכת נושא.

**עקרון**: אחידות. כל צבע/גודל בא ממשתנה ב-[style.css](style.css) (`:root`), לא מערך גולמי.
הקובץ כולו נכתב מחדש סביב הטוקנים; אין יותר זהב, ירוק מגרש, גרדיאנטים, מסגרות צבעוניות לפי
מדינה/ענף או שבבי תחרות רוויים.

### טוקנים
| טוקן | ערך | שימוש |
|---|---|---|
| `--brand-primary` | #2454E6 (קובלט) | פעולה ראשית, מצב נבחר, סיכות מפה. טקסט לבן עליו: 6.0:1 |
| `--brand-primary-hover` | #3A64F0 | ריחוף על רקע כהה (הערך #1D43B8 מהלוח מיועד לרקע בהיר) - **נגזר, לא מהלוח** |
| `--brand-ink` / `--surface` | #111827 | כרטיסים, פאנלים, דיאלוגים |
| `--brand-paper` / `--text` | #F8FAFC | טקסט ראשי; לוגו הפוך |
| `--bg` | #0B1220 | רקע דף וכותרת. **נגזר**: עמוק מ-ink כדי שסמל הקובלט יגיע ל-3.1:1 (על ink עצמו רק 2.94:1) |
| `--surface-2` | #1B2436 | שדות קלט, שבב שעה, מילוי משני - נגזר |
| `--surface-selected` | #16224A | כרטיס נבחר - נגזר (גוון קובלט על ink) |
| `--text-muted` | #9AA8BD | טקסט משני, 6.4-7.4:1 על כל המשטחים - נגזר (הערך #475569 מהלוח לרקע בהיר) |
| `--link` | #8CA5FF | טקסט/מסגרת נבחר על כהה. **הקובלט עצמו לא משמש לטקסט על ink** (~3:1) - נגזר |
| `--border-subtle` | #243044 | מפרידים דקורטיביים בלבד |
| `--border-control` | #64748B | גבול של שדה/כפתור: 3.3-3.9:1 |
| `--warn` / `--warn-soft` / `--focus` | #FCA5A5 / #3B1D22 / #8CA5FF | שגיאה/אזהרה וטבעת מיקוד, מוגדרים בנפרד מהמותג |

ניגודיות חושבה בפועל (סקריפט ב-2026-09-25), לא הונחה: טקסט 6.4:1 ומעלה, לבן על קובלט 6.02:1,
לבן על hover 4.93:1, גבולות שדות 3.26:1 ומעלה. גבול הכרטיס הנבחר עובר בכוונה על `--link` (הקובלט
על `--surface-selected` הוא רק 2.56:1). מצב נבחר לא נשען על צבע בלבד: "בטיול ✓", מסגרת, ומילוי.

### טיפוגרפיה, מרווחים, צורה
- **פונט אחד: Heebo** (עברית + לטינית), משקלים 400/500/600/700 לממשק. גוף 16px, שורה 1.55.
  משקל 900 נטען **רק** למילת הלוגו הזמנית (ראה למטה). ספרות טבלאיות (`tabular-nums`) בשעות,
  תאריכים ומרחקים. fallback: system-ui / Segoe UI.
- מרווחים 4/8/12/16/24/32px, רדיוס 12px (כרטיס) / 10px (שדה, כפתור), צל אחד (`--shadow`).
  בלי אנימציות מעבר מעבר ל-0.15s, ובלי תנועה כש-`prefers-reduced-motion`.

### הלוגו (כיוון A / FORWARD - מאושר)
- **הנכסים** (בתיקיית `brand/`) - **וקטור אמיתי** שסיפק המשתמש (2026-09-26): `tosport-logo-blue-white.svg`
  (וריאציה 04: סמל #2454E6, מילה #F8FAFC - בשימוש בכותרת) ו-`tosport-logo-ink-blue.svg` (וריאציה 02: סמל
  #111827, מילה #2454E6 - לרקע בהיר, לא בשימוש כרגע). המילה מומרת לנתיבים (outlined), הרקע שקוף.
  **השינוי היחיד שנעשה בקבצים**: ה-`viewBox` נחתך לאזור הציור (44 44 1968 428) כי הקנבס שנשלח כולל שוליים
  ריקים; הנתיבים לא נגעו. `favicon.svg` נבנה מנתיב הסמל של וריאציה 04 (ריבוע קובלט מעוגל + סמל בלבן), ו-
  `favicon.png` / `apple-touch-icon.png` (רסטר) נשארים כגיבוי לדפדפנים שלא תומכים ב-SVG favicon.
  ה-PNG-ים הזמניים של הלוגו והשחזור הידני של הסמל הוסרו.
- כותרת: `<h1><a><img alt="ToSport - דף הבית"></a></h1>` - שם נגיש אחד בלי כפילות; גובה 36px
  (30px בנייד), רוחב לפי היחס; מרווח חופשי 4px (יש לבדוק אופטית עם הווקטור הסופי).
- **מיפוי וריאציות לרקע** (לפי לוח הווריאציות):
  | וריאציה | סמל | מילה | רקע | קובץ |
  |---|---|---|---|---|
  | 01 ראשית | קובלט | ink | לבן/paper | (לעתיד, ערכת נושא בהירה) |
  | 02 | ink | קובלט | לבן/paper | - |
  | 03 | לבן | קובלט | ink, שימוש גדול | - (סופק, לא בשימוש) |
  | 04 | קובלט | לבן | ink, שימוש גדול | **`tosport-logo-blue-white.svg` - בכותרת** |
  | 05 הפוכה | לבן | לבן | קובלט או ink | (לא סופק כוקטור) |
  | 06 מונוכרומטית | ink | ink | לבן, הדפסה | - (סופק, לא בשימוש) |
  בכותרת בחרתי ב-04 על `--bg`, שנבחר כך שסמל הקובלט יגיע ל-3.1:1 (סף האלמנט הגרפי הוא 3:1).
  אם הוא נראה חלש - מחליפים ל-05 בשורה אחת.
- **פתוח**: בדיקת הסמל בלבד ב-16/24/32px, `apple-touch-icon`/PNG מהוקטור (כרגע מהרסטר הישן), ותמונת
  שיתוף (`og:image`). שאר הווריאציות (01, 03, 05, 06) עדיין לא סופקו כוקטור.

### רכיבים אחידים
כרטיס אירוע אחד (`cardHtml` ב-`app.js`) לרשימה ולפאנל המפה; תגית תחרות = לוגו על שבב לבן + שם, על
שבב ניטרלי; "הוסף לטיול" = כפתור מתאר (משני), נבחר = מלא בקובלט + ✓; אייקונים = SVG בקו (stroke
2), בלי אימוג'י; לוגואי קבוצות/תחרויות/מפה/קרדיטים לא נצבעו מחדש.

## שינוי שם

## בקוד (אוטומטי - אני עושה את זה)

שם המוצר, התיאור הקצר והגרסה יושבים במקום אחד: [brand.js](brand.js) (`name`, `descriptor`, `version`).
שינוי שם = כותרת הדף (`<title>`), מילת הלוגו בכותרת והגרסה בפוטר. שים לב: מילת הלוגו היא
כרגע טקסט זמני, ובלוגו הסופי (וקטור) השם יהיה חלק מהנכס עצמו - אז שינוי שם יחייב גם נכס חדש. וב-
`index.html` יש עותק של התיאור הקצר ב-`<meta name="description">` (סורקים לא מריצים JS).

לא נגעתי בכוונה בשם ב-User-Agent של הסקריפטים (`sports-trip-planner/1.0` ב-Python) - זה
מזהה את הפרויקט מול Nominatim/API-Football, לא שם פונה ללקוח, ואין סיבה לשנות אותו.
מסמכי ה-`.md` בתיקייה (README/ROADMAP/STATUS) מזכירים את השם הישן בטקסט חופשי - אני מעדכן אותם
יחד עם שינוי השם כשמבקשים.

## מחוץ לקוד (את אלה רק אתה יכול לעשות - חשבונות שלך)

### GitHub
1. Repo → Settings → General → Repository name → שם חדש.
2. **שים לב**: כתובת האתר (GitHub Pages) היא `telemiko23.github.io/<שם-הרפו>/` - היא תשתנה
   יחד עם שם הרפו, וה-URL הישן **לא** מנותב אוטומטית לחדש. (לגיט עצמו יש הפניה אוטומטית
   מהכתובת הישנה, לאתר לא.)
3. אחרי שינוי: להריץ אצלך `git remote set-url origin https://github.com/Telemiko23/<שם-חדש>.git`.
4. עדכון ה-URL של האתר ב-GA ו-Travelpayouts (למטה), ובקבצי STATUS.md.
5. **המלצה חזקה**: דומיין משלך (מחובר ל-GitHub Pages דרך CNAME, Settings → Pages → Custom
   domain) מנתק את המיתוג משם הרפו לחלוטין - אתר שנקרא `shem.com` לא תלוי ב-`github.io`,
   ובשינוי שם עתידי לא משנים כתובת. וזה גם מעלה מהימנות מול תוכניות שותפים (ראה
   הסירוב של Booking).

### Google Analytics 4
- Admin → Property settings → **Property name** (שם הנכס).
- Admin → Data streams → הסטרים → אפשר לערוך שם סטרים וכתובת האתר.
- ה-Measurement ID (`G-...`) **לא משתנה** - אין צורך לגעת בקוד.

### Travelpayouts
- **Project** = האתר שלך ב-Travelpayouts (Tools → Projects): שם ה-Project וה-URL שלו.
- שינוי ה-URL של Project שכבר הוגש לתוכניות עלול להפעיל בדיקה מחדש - כדאי לשנות שם/דומיין
  **לפני** בקשת חיבור חדשה ל-Booking, לא באמצע.
