# מיתוג המוצר - ToSport

## מערכת העיצוב (נקבעה 2026-09-25)

**עיקרון**: אחידות. כל צבע/גודל/משקל בא ממשתנה ב-[style.css](style.css) (`:root`), לא מערך גולמי.

**שם ומראה**: `ToSport` - "To" בלבן ו-"Sport" בזהב, תמיד LTR (גם בדף עברי), משקל 900.
הלוגו וה-favicon יגיעו מכלי חיצוני (בהמתנה); כשיגיעו: קובץ SVG בתיקיית הפרויקט, שורת
`<link rel="icon">` ב-`index.html`, והחלפת ה-wordmark בכותרת אם רוצים.

**פונט אחד: Heebo** (עברית + לטינית, משקלים 400/500/700/800/900). היררכיה במשקל ובגודל, לא
במשפחה: כותרת ראשית 900, כותרות משנה 700-800, גוף 400, תוויות/מטא 500-700.

**פלטה** (בהירה / כהה מתחלפות אוטומטית לפי המערכת):
| תפקיד | משתנה | בהיר | כהה |
|---|---|---|---|
| רקע דף | `--paper` | #F1EFE6 | #0C1512 |
| כרטיס | `--card` | #FFFFFF | #16211C |
| טקסט / משני | `--ink` / `--muted` | #132420 / #5E6E64 | #EAF1EC / #8FA098 |
| קו מפריד | `--line` | #DDD8C8 | #28342D |
| **ירוק מגרש** (ראשי) | `--pitch`, `--pitch-deep` | #0B4D33, #062F1F | #0B4D33, #062818 |
| ירוק פעולה | `--turf`, `--turf-deep`, `--turf-soft` | #1B8449, #0F5C38, #E3F0E8 | #4FC084, #6FDA9F, #17301F |
| **זהב** (הדגשה) | `--gold`, `--gold-deep`, `--gold-soft` | #B8862E, #8C6420, #F6ECD6 | #E0B45C, #F2CB7E, #2E260F |
| אזהרה | `--warn`, `--warn-soft` | #B03A2E, #FBE7E3 | #F28B80, #3A211D |

**צבע לפי ענף/מדינה** (פס הצד ותגית התחרות, `COUNTRY_COLOR` ב-`app.js`): כדורגל - צבע לכל
מדינה; פורמולה 1 אדום #D42A2A; טניס ירוק-ליים #7BA428; דארטס #C2185B.

**רכיבים אחידים**: כרטיס אירוע אחד (`cardHtml` ב-`app.js`) משמש גם את הרשימה וגם את פאנל
המפה; תגית תחרות = לוגו על שבב לבן + שם; כפתור ראשי = מסגרת ירוקה, נבחר = מלא; אייקונים =
SVG בקו (stroke 2, בלי אימוג'י); רדיוסים 10/12/16px; ללא אימוג'י בממשק.

## שינוי שם

## בקוד (אוטומטי - אני עושה את זה)

שם המוצר והגרסה יושבים במקום אחד: [brand.js](brand.js) (`name`, `mark` = שני חלקי ה-wordmark,
`tagline`, `version`). שינוי שם = כותרת הדף (`<title>`), הכותרת בראש האתר (החלק הראשון רגיל,
השני מודגש בצבע) והגרסה בפוטר - הכול מתעדכן. כל מה שצריך לומר לי: "שנה את שם המוצר ל-X".

לא נגעתי בכוונה בשם ב-User-Agent של הסקריפטים (`sports-trip-planner/1.0` ב-Python) - זה
מזהה את הפרויקט מול Nominatim/API-Football, לא שם פונה ללקוח, ואין סיבה לשנות אותו.
מסמכי ה-`.md` בתיקייה (README/ROADMAP/STATUS) מזכירים את השם הישן בטקסט חופשי - אני מעדכן אותם
יחד עם שינוי השם כשמבקשים.

## מחוץ לקוד (את אלה רק אתה יכול לעשות - חשבונות שלך)

### GitHub
1. Repo → Settings → General → Repository name → שם חדש.
2. **שים לב**: כתובת האתר (GitHub Pages) היא `telemiko23.github.io/<שם-הרפו>/` - היא תשתנה
   יחד עם שם הרפו, וה-URL הישן **לא** מנותב אוטומטית לחדש. (לגיט עצמו יש הפניה אוטומטית
   מהכתובת הישנה, לאתר לא.)
3. אחרי שינוי: להריץ אצלך `git remote set-url origin https://github.com/Telemiko23/<שם-חדש>.git`.
4. עדכון ה-URL של האתר ב-GA ו-Travelpayouts (למטה), ובקבצי STATUS.md.
5. **המלצה חזקה**: דומיין משלך (מחובר ל-GitHub Pages דרך CNAME, Settings → Pages → Custom
   domain) מנתק את המיתוג משם הרפו לחלוטין - אתר שנקרא `shem.com` לא תלוי ב-`github.io`,
   ובשינוי שם עתידי לא משנים כתובת. וזה גם מעלה מהימנות מול תוכניות שותפים (ראה
   הסירוב של Booking).

### Google Analytics 4
- Admin → Property settings → **Property name** (שם הנכס).
- Admin → Data streams → הסטרים → אפשר לערוך שם סטרים וכתובת האתר.
- ה-Measurement ID (`G-...`) **לא משתנה** - אין צורך לגעת בקוד.

### Travelpayouts
- **Project** = האתר שלך ב-Travelpayouts (Tools → Projects): שם ה-Project וה-URL שלו.
- שינוי ה-URL של Project שכבר הוגש לתוכניות עלול להפעיל בדיקה מחדש - כדאי לשנות שם/דומיין
  **לפני** בקשת חיבור חדשה ל-Booking, לא באמצע.
